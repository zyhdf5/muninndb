# Binary assets - downloaded via `make fetch-assets`
# Do not commit these large binary files to the repository.
# Run `make fetch-assets` to download them before building with local embedder support.
*.onnx
*.so
*.so.*
*.dylib
*.dll
tokenizer.json
*.tgz
*.zip
